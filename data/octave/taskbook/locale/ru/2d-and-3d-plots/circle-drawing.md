Написать программу, которая строит окружность радиуса `r` из `n` отрезков с помощью функции `plot`. Значения `r` и `n` вводятся с клавиатуры.
Значения точек получившейся окружности записать в массив `result`.
